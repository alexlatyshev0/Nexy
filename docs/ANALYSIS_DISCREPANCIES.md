# Анализ расхождений между документацией и реализацией

**Дата анализа:** 2025-01-20  
**Анализируемые документы:**
- `docs/architecture.md` - Discovery v2 Design Specification
- `docs/status.md` - V2 Implementation Status
- `docs/scenes.md` - Scenes System
- `docs/database.md` - Database Schema V2
- `docs/onboarding-integration.md` - Интеграция визуального онбординга
- `docs/body-map.md` - Body Map System
- `docs/admin-panel.md` - Admin Panel Documentation
- `nexy_visual_onboarding_spec.md` - Визуальный онбординг
- `intimate-discovery-v2.md` - Техническое задание

---

## 🔴 КРИТИЧЕСКИЕ РАСХОЖДЕНИЯ

### 1. Визуальный онбординг: реализация отличается от спецификации

> ✅ **РЕШЕНО** - архитектура улучшена в миграции 020

**Текущая реализация:**
- ✅ Сцены в таблице `scenes` с `category = 'onboarding'` (миграция 017)
- ✅ **Явная колонка `onboarding_category`** хранит категорию (миграция 020)
- ✅ **Колонка `onboarding_direction`** для give/receive (миграция 020)
- ✅ 20 категорий: 16 базовых + 4 условных
- ✅ Условная логика через `onboarding_conditional` и `onboarding_condition`

**Что было сделано (миграция 020):**
- Добавлена колонка `onboarding_category` — явное хранение категории
- Добавлена колонка `onboarding_direction` — направление (give/receive/null)
- Код в `visual-onboarding/page.tsx` использует новую колонку с fallback на парсинг slug
- Решена проблема с multi-word категориями (body-fluids → body_fluids)

**Документация (требует обновления):**
- ⚠️ `nexy_visual_onboarding_spec.md` описывает старые 24 категории
- ⚠️ `docs/onboarding-integration.md` использует смешанные названия

---

### 2. Категории онбординга: расхождение в названиях

> ✅ **РЕШЕНО** - актуальная система: **20 категорий с объединёнными названиями**

**Актуальная реализация (TypeScript):**
- ✅ **16 базовых:** `oral`, `anal`, `group`, `toys`, `roleplay`, `quickie`, `romantic`, `power_dynamic`, `rough`, `public`, `exhibitionism`, `recording`, `dirty_talk`, `praise`, `lingerie`, `foot`
- ✅ **4 условных:** `bondage`, `body_fluids`, `sexting`, `extreme`
- ✅ Источник: `src/lib/onboarding-gates.ts` → `OnboardingResponses` interface

**Устаревшие названия (не используются):**
- ❌ `oral_give`, `oral_receive` → заменены на `oral`
- ❌ `anal_give`, `anal_receive` → заменены на `anal`
- ❌ `dom_sub`, `dom_dom` → заменены на `power_dynamic`
- ❌ `cum_play` → заменено на `body_fluids`

**Проблема в документации:**
- `nexy_visual_onboarding_spec.md` всё ещё описывает старые 24 категории
- `docs/onboarding-integration.md` использует смешанные названия
- Нужно обновить документацию под актуальную систему

---

### 3. Body Map: количество активностей

> ✅ **РЕШЕНО** - legacy-код удалён

**Текущая реализация:**
- Body Map использует виртуальные сцены: `bodymap-self`, `bodymap-partner-female`, `bodymap-partner-male`
- Сцены создаются динамически на основе профиля пользователя
- Zone-first режим: сначала выбор зоны, потом активности

**Что было сделано:**
- Удалена неиспользуемая константа `BODY_MAP_ACTIVITIES` из `src/app/(app)/discover/page.tsx`
- Константа содержала 6 legacy-активностей (kissing, licking, light-touch, light-slapping, biting, spanking), которые нигде не использовались

---

## ⚠️ ЗНАЧИТЕЛЬНЫЕ РАСХОЖДЕНИЯ

### 4. Composite Scenes: количество сцен

> ✅ **РЕШЕНО** - актуальное количество: **135 composite scenes**

**Реализация (`scenes/v2/composite/_index.json`):**
- ✅ `total_scenes: 135` в метаданных
- ✅ 28 категорий сцен (baseline, oral, anal, group, etc.)
- ✅ 14 baseline + 121 detailed = 135 total

**Документация:**
- ✅ `docs/architecture.md` — корректно указывает 135 сцен
- ⚠️ `docs/_archive/ANALYSIS_REPORT.md` — устаревшие 94 сцены (архив)

**Примечание:**
- Цифра "94 scenes" из архивных документов, количество увеличилось до 135
- Архивные документы не требуют обновления (помечены как `_archive`)

---

### 5. Gates система: два разных подхода

> ✅ **РЕШЕНО** в миграции `019_unified_gates.sql`

**Было:**
- Два места с логикой вычисления гейтов (SQL и TypeScript)
- SQL-функция `compute_onboarding_gates()` не использовалась
- Клиент вычислял гейты через `computeGates()` и сохранял в БД

**Стало:**
- **Single source of truth:** таблица `user_gates`
- Гейты вычисляются **автоматически** триггерами при изменении:
  - `onboarding_responses` → `trg_recompute_gates_on_onboarding`
  - `body_map_responses` → `trg_recompute_gates_on_body_map`
- Клиент только **читает** гейты через `fetchUserGates()`

**Удалено (cleanup):**
- Колонка `onboarding_responses.gates`
- SQL функции `compute_onboarding_gates()`, `is_scene_gated()`
- TypeScript функция `computeGates()`

**Использование:**
```typescript
// Сохранение (только responses, gates вычислятся автоматически)
await supabase.from('onboarding_responses').upsert({ responses, ... });

// Чтение
const gates = await fetchUserGates(supabase, userId);
const allowed = isSceneAllowed('blowjob', gates);
```

---

### 6. Сохранение ответов: несколько таблиц

> ✅ **РЕШЕНО** - таблицы объединены в миграции 021

**Что было сделано (миграция 021):**
- Таблица `composite_scene_responses` удалена
- Все данные перенесены в `scene_responses`
- Добавлены колонки: `scene_slug`, `element_responses`, `skipped`, `updated_at`

**Текущая архитектура — одна таблица `scene_responses`:**

| Колонка | Тип | Назначение |
|---------|-----|------------|
| `scene_id` | UUID | FK на scenes.id |
| `scene_slug` | TEXT | Slug для V2 алгоритмов |
| `elements_selected` | TEXT[] | Выбранные элементы |
| `element_responses` | JSONB | Follow-up ответы по элементам |
| `skipped` | BOOLEAN | Пропущена ли сцена |

**Обновлённый код:**
- `scene-progression.ts` — использует `scene_slug` для V2 логики
- `discover/page.tsx` — пишет в одну таблицу
- `partner-chat/route.ts` — читает из `scene_responses`

---

### 7. Scene Progression: функции документации

**Документация (`docs/status.md`):**
- ✅ `getAdaptiveScenes()` - адаптивная приоритизация
- ✅ `calculateSceneScore()` - scoring сцен
- ✅ Baseline Gates Filtering
- ✅ Comfort Signals / Intensity Progression
- ✅ Exploration vs Exploitation
- ✅ Breadth-First Category Coverage

**Реализация (`src/lib/scene-progression.ts`):**
- Все функции реализованы
- НО: документация не описывает параметры и детали работы
- Неясно, как эти функции интегрированы в основной flow

**Проблема:**
- Реализация существует, но документация поверхностная

---

## 📝 НЕЗНАЧИТЕЛЬНЫЕ РАСХОЖДЕНИЯ / НЕЯСНОСТИ

### 8. Landing Page спецификация

**Документация (`landing-page-spec.md`):**
- Описан полный дизайн лендинга
- Структура страницы, компоненты, цвета

**Реализация:**
- Не найдено реализации лендинга
- Либо не реализовано, либо находится в другой части проекта

---

### 9. Admin Panel: миграции

**Документация (`docs/admin-panel.md`):**
- Упоминает миграции: `006_add_missing_columns.sql`, `008_add_accepted_field.sql`
- Описывает, что эти миграции добавляют поля для QA и промптов

**Реализация:**
- В папке migrations есть: `005_scenes_v2_composite.sql`, `011_finalize_v2.sql`, `015_visual_onboarding.sql`, и др.
- НО: нет `006_add_missing_columns.sql` или `008_add_accepted_field.sql`
- Вероятно, функционал был включён в другие миграции (005 или 011)

**Проблема:**
- Документация ссылается на несуществующие миграции
- Неясно, в какой миграции на самом деле добавлены эти поля
- Нужно проверить `005_scenes_v2_composite.sql` и `011_finalize_v2.sql` на наличие полей: `generation_prompt`, `qa_status`, `qa_attempts`, `qa_last_assessment`, `prompt_instructions`, `accepted`

---

### 10. Show-If условия

**Документация (`docs/onboarding-integration.md`):**
- Описывает систему `skip_if`, `prefill_if`, `show_only_if`
- Файл `src/lib/scene-conditions.ts` упоминается

**Документация (`docs/status.md`):**
- Правильно указывает `src/lib/show-if.ts`

**Реализация (`src/lib/show-if.ts`):**
- Файл существует с именем `show-if.ts`
- Функции: `shouldShowFollowUp()`, `filterFollowUps()`
- Реализует только `show_if` для follow-ups, но НЕ реализует `skip_if` и `prefill_if` для элементов сцен

**Проблема:**
- `onboarding-integration.md` упоминает несуществующий файл `scene-conditions.ts`
- Документация описывает `skip_if` и `prefill_if`, но реализация их не содержит
- Функционал `skip_if`/`prefill_if` для элементов сцен не реализован

---

## ✅ ЧТО РЕАЛИЗОВАНО, НО НЕ ДОКУМЕНТИРОВАНО

### 11. Дополнительные компоненты UI

**Реализация:**
- `ExclusionDialog` - диалог исключения категорий
- `BodyMapAnswer` - компонент для body map (документирован в `body-map.md`)
- `FollowUpFlow` - компонент для follow-up вопросов (упоминается в status.md)

**Документация:**
- `ExclusionDialog` не описан нигде

---

### 12. Дополнительные модули

**Реализация:**
- `src/lib/show-if.ts` - существует, реализует только `show_if` для follow-ups
- `src/lib/profile-signals.ts` - упоминается в коде, документация минимальная
- `src/lib/partner-archetypes.ts` - существует, не документирован
- `src/lib/matching.ts` - существует, не документирован

**Проблема:**
- `skip_if` и `prefill_if` для элементов сцен не реализованы (только описаны в документации)

---

### 13. Миграции базы данных

**Реализация (файлы в `supabase/migrations/`):**
- `012_add_language_to_profiles.sql` - добавление языка в профиль
- `013_fix_excluded_scene_ids.sql` - исправление функции исключений
- `014_cleanup_legacy_fields.sql` - очистка legacy полей
- `016_security_fixes.sql` - исправления безопасности
- `017_onboarding_as_scenes.sql` - онбординг как сцены
- `018_onboarding_paired_with.sql` - парные сцены онбординга

**Документация:**
- Упоминаются только основные миграции (005, 011, 015)
- Дополнительные миграции не описаны

---

## 🔍 РЕКОМЕНДАЦИИ

### Приоритет 1 (Критично)
1. ~~**Унифицировать описание системы онбординга**~~ ✅ РЕШЕНО
   - ~~Определить актуальную структуру категорий~~
   - ~~Обновить все документы под одну версию~~
   - **Решено:** миграция 020 добавила явные колонки `onboarding_category` и `onboarding_direction`

2. ~~**Уточнить количество сцен**~~ ✅ РЕШЕНО
   - ~~Подсчитать фактическое количество сцен в БД~~
   - **Решено:** 135 composite scenes (подтверждено в `_index.json`)

3. ~~**Документировать двойное сохранение ответов**~~ ✅ РЕШЕНО
   - ~~Объяснить, почему сохраняется и в `composite_scene_responses`, и в `scene_responses`~~
   - **Решено:** это осознанная архитектура (slug vs UUID ключи, разные use cases)

### Приоритет 2 (Важно)
4. **Документировать все миграции**
   - Добавить описание всех миграций в `docs/database.md`
   - Указать, что было изменено в каждой

5. ~~**Документировать клиентские функции гейтов**~~ ✅ РЕШЕНО
   - ~~Объяснить, зачем дублируется логика в TypeScript и SQL~~
   - **Решено:** миграция 019 унифицировала систему, клиент только читает из `user_gates`

6. **Документировать ExclusionDialog**
   - Описать функционал исключения категорий
   - Объяснить, как это интегрируется в flow

### Приоритет 3 (Желательно)
7. ~~**Уточнить legacy-код**~~ ✅ РЕШЕНО
   - ~~Объяснить, используется ли `BODY_MAP_ACTIVITIES`~~
   - ~~Если нет - удалить или пометить как deprecated~~
   - **Удалено:** константа `BODY_MAP_ACTIVITIES` удалена из кода

8. **Документировать дополнительные модули**
   - `partner-archetypes.ts`
   - `matching.ts`
   - `profile-signals.ts` (более детально)

9. **Исправить несоответствие имён файлов**
   - Переименовать `show-if.ts` в `scene-conditions.ts` или обновить документацию

---

## 📊 СТАТИСТИКА РАСХОЖДЕНИЙ

| Категория | Количество | Решено |
|-----------|------------|--------|
| Критические расхождения | 3 | 3 ✅ (Body Map, Онбординг, Категории) |
| Значительные расхождения | 4 | 3 (Gates, Composite Scenes, Сохранение ответов) |
| Незначительные / Неясности | 3 | 0 |
| Реализовано, но не документировано | 3 | 0 |

**Итого:** 13 пунктов, **6 решено**, 7 осталось

---

## 🔄 ПРОЦЕСС ОБНОВЛЕНИЯ

Рекомендуется:
1. Сначала исправить критические расхождения
2. Затем добавить недостающую документацию
3. После этого обновить существующие документы для соответствия реализации
