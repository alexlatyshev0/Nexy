# Интеграция визуального онбординга

## 0. Архитектура системы гейтов

> **Важно:** Начиная с миграции `019_unified_gates.sql`, гейты вычисляются **автоматически** в базе данных.

### Принцип Single Source of Truth

```
┌─────────────────────┐     ┌─────────────────────┐
│ onboarding_responses│     │ body_map_responses  │
│   responses JSONB   │     │   activity_id, zones│
└─────────┬───────────┘     └─────────┬───────────┘
          │ TRIGGER                   │ TRIGGER
          ▼                           ▼
┌─────────────────────────────────────────────────┐
│              user_gates (единая таблица)         │
│  ├─ onboarding_gates JSONB                      │
│  ├─ body_map_gates JSONB                        │
│  └─ gates JSONB ← ЕДИНЫЙ ИСТОЧНИК для запросов  │
└─────────────────────────────────────────────────┘
```

### Как это работает

1. **Клиент сохраняет только ответы** (responses), не вычисляет гейты
2. **Триггеры автоматически** вызывают `recompute_user_gates()` при:
   - INSERT/UPDATE в `onboarding_responses`
   - INSERT/UPDATE/DELETE в `body_map_responses`
3. **Клиент читает готовые гейты** из `user_gates.gates`

### Использование в TypeScript

```typescript
import { fetchUserGates, isSceneAllowed } from '@/lib/onboarding-gates';

// Получить гейты из БД (не вычислять!)
const gates = await fetchUserGates(supabase, userId);

// Проверить доступ к сцене
const allowed = isSceneAllowed('blowjob', gates);
```

### Сохранение ответов онбординга

```typescript
// ПРАВИЛЬНО: сохраняем только responses, gates вычислятся триггером
await supabase
  .from('onboarding_responses')
  .upsert({
    user_id: user.id,
    responses: { oral: 1, rough: 2, ... },
    completed: true
  });

// НЕПРАВИЛЬНО: клиент НЕ должен вычислять gates
// const gates = computeGates(responses);  // DEPRECATED
```

### SQL функции

| Функция | Назначение |
|---------|------------|
| `compute_gates_from_onboarding(responses)` | Вычисляет onboarding gates |
| `compute_gates_from_body_map(user_id)` | Вычисляет body map gates |
| `recompute_user_gates(user_id)` | Пересчитывает все gates для юзера |
| `is_scene_gated_v2(user_id, slug, gates[], op, level)` | Проверяет доступ к сцене |

### Cleanup (миграция 019)

Удалено:
- Колонка `onboarding_responses.gates` (теперь в `user_gates`)
- SQL функция `compute_onboarding_gates()`
- SQL функция `is_scene_gated()` → заменена на `is_scene_gated_v2()`
- TypeScript функция `computeGates()` — удалена

---

## 1. Маппинг: Категории онбординга → Наши сцены

### Принцип
Онбординг даёт **грубый профиль** (24 категории, 3 реакции).
Наши сцены дают **детальный профиль** (162 сцены, множество опций).

Онбординг = **гейт** (открывает/закрывает доступ к группам сцен).

---

### ORAL_GIVE (Оральный секс — давать)

**Если YES/VERY → открываются:**
- `blowjob` — базовый минет
- `deepthroat` — глубокий минет
- `cunnilingus` — куннилингус
- `cock-worship` — поклонение члену
- `pussy-worship` — поклонение пизде
- `facesitting-f-on-m` — она садится на лицо
- `facesitting-m-on-f` — он садится на лицо
- `rimming-m-to-f` — анилингус ей
- `rimming-f-to-m` — анилингус ему

**Гейтит также:**
- `cum_play` категорию (если oral_give = NO → cum_play скрыт)

---

### ORAL_RECEIVE (Оральный секс — получать)

**Связан с теми же сценами, но с другой перспективы.**
Если человек сказал YES на oral_give но NO на oral_receive — это важная инфо: любит давать, не получать.

---

### MUTUAL_TOUCH (Мастурбация партнёру)

**Если YES/VERY → открываются:**
- `handjob` — дрочка ему
- `fingering` — фингеринг ей
- `edging-m-to-f` — эджинг ей
- `edging-f-to-m` — эджинг ему
- `forced-orgasm-m-to-f` — принудительный оргазм ей
- `forced-orgasm-f-to-m` — принудительный оргазм ему
- `ruined-orgasm-f-to-m` — испорченный оргазм ему
- `ruined-orgasm-m-to-f` — испорченный оргазм ей

---

### ANAL_GIVE (Анальный секс — активная роль)

**Если YES/VERY → открываются:**
- `anal-play-on-her` — анал ей
- `anal-play-on-him` — анал ему
- `pegging` — пеггинг (она ему со страпоном)
- `double-penetration` — двойное проникновение

---

### ANAL_RECEIVE (Анальный секс — пассивная роль)

**Те же сцены, другая перспектива.**
Важно для понимания: готов ли человек принимать.

---

### GROUP (Групповой секс)

**Если YES/VERY → открываются:**
- `threesome-fmf` — тройничок МЖЖ
- `threesome-mfm` — тройничок ММЖ
- `gangbang` — гангбанг
- `orgy` — оргия
- `swinging` — свинг
- `double-penetration` — двойное проникновение
- `cuckold` — куколд
- `hotwife` — хотвайф

---

### TOYS (Игрушки)

**Если YES/VERY → открываются:**
- `vibrator` — вибратор
- `remote-control` — дистанционное управление
- `butt-plug` — анальная пробка
- `cock-ring` — кольцо на член
- `dildo` — дилдо
- `nipple-clamps` — зажимы для сосков
- `fucking-machine` — секс-машина

---

### ROLEPLAY (Ролевые игры)

**Если YES/VERY → открываются:**
- `boss-m-secretary-f` — босс М и секретарша Ж
- `boss-f-subordinate-m` — босс Ж и подчинённый М
- `teacher-m-student-f` — учитель М и студентка Ж
- `teacher-f-student-m` — учительница Ж и студент М
- `doctor-patient` — доктор и пациент
- `stranger` — незнакомец
- `service-roleplay` — сервисный ролеплей
- `taboo-roleplay` — табу ролеплей
- `ddlg` — daddy dom / little girl
- `mdlb` — mommy dom / little boy
- `pet-play-f-is-pet` — пет-плей, она питомец
- `pet-play-m-is-pet` — пет-плей, он питомец

---

### FOREPLAY (Долгие прелюдии)

**Если YES/VERY → открываются:**
- `massage-m-to-f` — массаж ей
- `massage-f-to-m` — массаж ему
- `body-worship-m-to-f` — поклонение телу ей
- `body-worship-f-to-m` — поклонение телу ему
- `feather-tickle` — щекотание пёрышком
- `ice-play` — игра со льдом
- `blindfold` — повязка на глаза
- `casual-touch` — случайные прикосновения
- `morning-teasing` — утренние дразнилки

---

### QUICKIE (Быстрый секс)

**Если YES/VERY → открываются:**
- `quickie` — быстрый секс
- `kitchen-counter` — на кухне
- `public-sex` — секс в публичных местах
- `angry-sex` — секс в гневе

---

### ROMANTIC (Романтика)

**Если YES/VERY → открываются:**
- `romantic-sex` — романтический секс
- `emotional-sex` — эмоциональный секс
- `first-time-together` — первый раз вместе
- `makeup-sex` — секс после ссоры
- `aftercare` — забота после

---

### CUM_PLAY (Игра со спермой)

**Гейт:** Показывается только если oral_give ≠ NO

**Если YES/VERY → открываются:**
- `cum-where-to-finish` — куда кончить
- `squirting` — сквирт (женский)
- `squirt-receiving` — принимать сквирт
- `breeding-kink` — бридинг кинк

---

### DOM_SUB (Подчинение — быть сабом)

**Если YES/VERY → открываются:**
- `collar-m-owns-f` — ошейник, он владеет ей
- `collar-f-owns-m` — ошейник, она владеет им
- `free-use-f-available` — free use, она доступна
- `free-use-m-available` — free use, он доступен
- `objectification-f` — объективация её
- `objectification-m` — объективация его
- `feminization` — феминизация
- `chastity-m-locked` — пояс верности на нём
- `chastity-f-locked` — пояс верности на ней

---

### DOM_DOM (Доминирование — быть домом)

**Те же сцены, но с перспективы доминанта.**
Если YES на dom_dom но NO на dom_sub — чистый дом.
Если YES на оба — свитч.

---

### BONDAGE (Бондаж/связывание)

**Гейт:** Показывается только если хотя бы один из dom_sub/dom_dom/rough ≠ NO

**Если YES/VERY → открываются:**
- `bondage-m-ties-f` — он связывает её
- `bondage-f-ties-m` — она связывает его
- `mummification-f` — мумификация её
- `mummification-m` — мумификация его

---

### ROUGH (Грубый секс)

**Если YES/VERY → открываются:**
- `choking-m-to-f` — удушение ей
- `choking-f-to-m` — удушение ему
- `face-slapping-m-to-f` — пощёчина ей
- `face-slapping-f-to-m` — пощёчина ему
- `spanking-m-to-f` — порка ей
- `spanking-f-to-m` — порка ему
- `primal` — праймал
- `cnc-m-takes-f` — CNC, он берёт её
- `cnc-f-takes-m` — CNC, она берёт его
- `somnophilia-m-to-f` — сомнофилия, он к ней
- `somnophilia-f-to-m` — сомнофилия, она к нему

**Также гейтит extreme категорию:**
- `breath-play-m-to-f`, `breath-play-f-to-m`
- `knife-play-m-to-f`, `knife-play-f-to-m`
- `whipping-m-to-f`, `whipping-f-to-m`

---

### PUBLIC (Секс вне спальни)

**Если YES/VERY → открываются:**
- `public-sex` — секс в публичных местах
- `locations` — разные локации
- `kitchen-counter` — на кухне

---

### EXHIBITIONISM (Показать себя)

**Если YES/VERY → открываются:**
- `exhibitionism` — эксгибиционизм
- `striptease-f` — стриптиз она
- `striptease-m` — стриптиз он
- `glory-hole-f-gives` — глори хол, она даёт
- `glory-hole-m-gives` — глори хол, он даёт

---

### RECORDING (Съёмка интима)

**Если YES/VERY → открываются:**
- `filming` — съёмка
- `sexting` — секстинг

---

### DIRTY_TALK (Грязные разговоры)

**Если YES/VERY → открываются:**
- `dirty-talk` — грязные разговоры
- `degradation-m-to-f` — унижение ей
- `degradation-f-to-m` — унижение ему

---

### PRAISE (Похвалы и восхищение)

**Если YES/VERY → открываются:**
- `praise-m-to-f` — похвала ей
- `praise-f-to-m` — похвала ему

---

### LINGERIE (Красивое бельё)

**Если YES/VERY → открываются:**
- `lingerie-f` — бельё на ней
- `lingerie-m` — бельё на нём
- `stockings` — чулки
- `heels-only` — только каблуки
- `harness-f` — харнесс на ней
- `harness-m` — харнесс на нём
- `latex-leather` — латекс и кожа
- `uniforms-f` — униформа на ней
- `uniforms-m` — униформа на нём
- `torn-clothes` — рваная одежда

---

### FOOT (Фут-фетиш)

**Если YES/VERY → открываются:**
- `foot-worship-m-to-f` — поклонение ногам ей
- `foot-worship-f-to-m` — поклонение ногам ему

---

### SEXTING (Секстинг/интим-фото)

**Если YES/VERY → открываются:**
- `sexting` — секстинг
- `filming` — съёмка
- `joi` — JOI (инструкции для мастурбации)

---

## 2. Body Map — куда и зачем

### Текущая структура Body Map

6 активностей × 2 направления (give/receive):
1. **Kissing** (целовать) — intensity 1
2. **Licking** (лизать) — intensity 2
3. **Light-touch** (лёгкие касания) — intensity 1
4. **Light-slapping** (лёгкие шлепки) — intensity 2
5. **Biting** (кусать) — intensity 3
6. **Spanking** (порка) — intensity 4

### Когда показывать Body Map?

**Вариант A: После онбординга, до детальных сцен**
```
Онбординг (свайпы)
    → Body Map
    → Детальные сцены
```

**Вариант B: Параллельно с онбордингом (часть свайпов)**
Добавить 2-3 body map вопроса в онбординг как отдельные "сцены".

**Рекомендация: Вариант A** — body map это отдельный этап.

### Выводы из Body Map → Категории онбординга

| Body Map действие | Зона | Вывод для онбординга |
|-------------------|------|---------------------|
| Licking | genitals (give) | `oral_give` = YES |
| Licking | genitals (receive) | `oral_receive` = YES |
| Spanking | ass (give) | `rough` = YES, `dom_dom` намёк |
| Spanking | ass (receive) | `rough` = YES, `dom_sub` намёк |
| Light-slapping | face (give) | `rough` = VERY |
| Light-slapping | face (receive) | `rough` = VERY, `dom_sub` намёк |
| Biting | neck (receive) | `rough` = YES (лёгкий) |
| Kissing | feet (give) | `foot` = YES |
| Licking | feet (give) | `foot` = VERY |
| Kissing | everywhere | `foreplay` = YES, `romantic` = YES |

### Обратная связь: Онбординг → Body Map

Если в онбординге человек сказал NO на `rough`:
- Body map вопросы про spanking, biting, light-slapping можно **пропустить** или показать с низким приоритетом

Если в онбординге человек сказал VERY на `foot`:
- Body map вопросы про feet можно **подсветить** или показать первыми

---

## 3. Адаптированные категории онбординга

### Изменения относительно исходного документа

| Исходная категория | Адаптация | Причина |
|-------------------|-----------|---------|
| `dom_sub` + `dom_dom` | Объединить в `power_dynamic` с 4 опциями: dom / sub / switch / vanilla | У нас это одна baseline сцена |
| `bondage` | Оставить, но показывать только если power ≠ vanilla | Согласуется с нашей логикой |
| `cum_play` | Переименовать в `body_fluids` | У нас это целая категория |
| `foreplay` | Убрать как отдельную категорию | Это часть `romantic` + body map покрывает |
| `mutual_touch` | Убрать | Слишком базовое, все это делают |

### Финальный список категорий (20)

**Базовые (обязательные для всех) — 16:**
1. `oral` — оральный секс (объединить give/receive в один свайп)
2. `anal` — анальный секс
3. `group` — групповой секс
4. `toys` — игрушки
5. `roleplay` — ролевые игры
6. `quickie` — быстрый секс
7. `romantic` — романтика
8. `power_dynamic` — власть (4 варианта вместо 2х2)
9. `rough` — грубый секс
10. `public` — секс вне спальни
11. `exhibitionism` — показать себя
12. `recording` — съёмка
13. `dirty_talk` — грязные разговоры
14. `praise` — похвалы
15. `lingerie` — красивое бельё
16. `foot` — фут-фетиш

**Условные (по правилам) — 4:**
17. `bondage` — бондаж (если power ≠ vanilla ИЛИ rough = YES)
18. `body_fluids` — жидкости (если oral = YES)
19. `sexting` — секстинг (если recording = YES ИЛИ exhibitionism = YES)
20. `extreme` — экстрим (если rough = VERY И bondage = YES)

---

## 4. Гейты и unlock логика

### 4.1 ПОЛНАЯ МАТРИЦА ГЕЙТОВ (все 162 сцены)

| Сцена | Гейт | Логика |
|-------|------|--------|
| **ORAL** | | |
| blowjob | `oral` | |
| cunnilingus | `oral` | |
| deepthroat | `oral` | может быть чувственным |
| facesitting-f-on-m | `oral` | |
| facesitting-m-on-f | `oral` | |
| rimming-m-to-f | `oral` | это лизать, не проникновение |
| rimming-f-to-m | `oral` | это лизать, не проникновение |
| **ANAL** | | |
| anal-play-on-her | `anal` | |
| anal-play-on-him | `anal` | |
| pegging | `anal` + `power` | |
| butt-plug | `anal` ИЛИ `toys` | любой |
| figging | `anal` + `rough` | extreme |
| **MANUAL** | | |
| handjob | — | всегда |
| fingering | — | всегда |
| titfuck | — | всегда |
| **BODY FLUIDS** | | |
| cum-where-to-finish | `body_fluids` | |
| squirting | `body_fluids` | F-перспектива |
| squirt-receiving | `body_fluids` + `oral` | M хочет получить — нужны ОБА |
| golden-shower-* | `body_fluids` | табу, не обязательно power |
| spitting-* | `body_fluids` + `power` | плевать = доминация |
| breeding-kink | `body_fluids` | |
| **WORSHIP** | | |
| cock-worship | `oral` | |
| pussy-worship | `oral` | |
| body-worship-* | `romantic` | |
| foot-worship-* | `foot` | |
| armpit | `body_fluids` ИЛИ `rough` | специфичный |
| **IMPACT/PAIN** | | |
| spanking-* | `rough` | |
| choking-* | `rough` | |
| face-slapping-* | `rough` = VERY | только сильный |
| whipping-* | `rough` + `bondage` | нужны ОБА |
| wax-play-* | `rough` ИЛИ `toys` | любой |
| nipple-play-* | `rough` ИЛИ `toys` | любой |
| cbt | `rough` + `power` | cock & ball torture |
| **VERBAL** | | |
| dirty-talk | `dirty_talk` | |
| degradation-* | `dirty_talk` + `power` | нужны ОБА |
| praise-* | `praise` | |
| **CONTROL/POWER** | | |
| bondage-* | `bondage` | |
| collar-* | `power` = dom/sub | |
| edging-* | `power` ИЛИ `toys` | любой |
| forced-orgasm-* | `power` | контроль, не грубость |
| ruined-orgasm-* | `power` | контроль/тизинг |
| free-use-* | `power` | |
| objectification-* | `power` | дегуманизация = power |
| chastity-* | `power` | |
| feminization | `power` = sub (M) | |
| **CNC/ROUGH** | | |
| cnc-* | `rough` = VERY | только сильный |
| primal | `rough` | |
| somnophilia-* | `power` | CNC-adjacent, контроль |
| **EXTREME** | | |
| breath-play-* | `rough` = VERY + `bondage` | extreme |
| knife-play-* | `rough` = VERY + `bondage` | extreme |
| mummification-* | `bondage` = VERY | только сильный |
| needle-play | `rough` = VERY + `bondage` | extreme |
| fisting-* | `anal` = VERY ИЛИ `power` | extreme |
| fucking-machine | `toys` | просто игрушка |
| lactation | — | показывать если F кормящая |
| electrostim | `toys` | сенсорное, не грубое |
| **GROUP** | | |
| threesome-fmf | `group` | |
| threesome-mfm | `group` | |
| gangbang | `group` = VERY | только сильный |
| orgy | `group` = VERY | только сильный |
| swinging | `group` | |
| double-penetration | `group` + `anal` | нужны ОБА |
| **CUCKOLD** | | |
| cuckold | `group` + `power` = sub (M) | |
| hotwife | `group` + (`power` ИЛИ `exhibitionism`) | |
| **EXHIBITIONISM** | | |
| exhibitionism | `exhibitionism` | |
| voyeurism | `exhibitionism` | |
| public-sex | `public` | |
| glory-hole-* | `exhibitionism` + `oral` | нужны ОБА |
| striptease-* | `exhibitionism` | |
| **ROLEPLAY** | | |
| boss-*-* | `roleplay` | |
| teacher-*-* | `roleplay` | |
| doctor-patient | `roleplay` | |
| stranger | `roleplay` | |
| service-roleplay | `roleplay` + `power` | |
| taboo-roleplay | `roleplay` = VERY | только сильный |
| **PET/AGE PLAY** | | |
| pet-play-* | `roleplay` + `power` | нужны ОБА |
| ddlg | `roleplay` + `power` | |
| mdlb | `roleplay` + `power` | |
| **SENSORY** | | |
| blindfold | `power` ИЛИ `romantic` | любой |
| ice-play | `romantic` ИЛИ `toys` | любой |
| feather-tickle | `romantic` | |
| **TOYS** | | |
| vibrator | `toys` | |
| dildo | `toys` | |
| cock-ring | `toys` | |
| nipple-clamps | `toys` + `rough` | нужны ОБА |
| remote-control | `toys` + `public` | нужны ОБА |
| **CLOTHING** | | |
| lingerie-* | `lingerie` | |
| stockings | `lingerie` | |
| heels-only | `lingerie` | |
| harness-* | `lingerie` | может быть эстетика |
| latex-leather | `lingerie` | может быть эстетика |
| uniforms-* | `lingerie` + `roleplay` | нужны ОБА |
| torn-clothes | `rough` | страсть, не эстетика белья |
| **FILMING** | | |
| filming | `recording` | |
| sexting | `recording` ИЛИ `exhibitionism` | любой |
| joi | `recording` ИЛИ `power` | любой |
| **ROMANTIC** | | |
| romantic-sex | `romantic` | |
| emotional-sex | `romantic` | |
| aftercare | `romantic` ИЛИ `power` | любой |
| quickie | `quickie` | |
| first-time-together | `romantic` | |
| makeup-sex | `romantic` | |
| angry-sex | `rough` ИЛИ `quickie` | любой |
| **INTIMACY** | | |
| casual-touch | — | всегда |
| morning-teasing | — | всегда |
| kitchen-counter | `public` ИЛИ `quickie` | любой |
| **MASSAGE** | | |
| massage-* | `romantic` | |
| **LGBTQ** | | |
| wlw | ориентация = лесби/би-Ж | |
| mlm | ориентация = гей/би-М | |

---

### 4.2 Body Map → Детальные сцены (условная логика)

Body Map не только подтверждает онбординг, но и **пропускает/предзаполняет вопросы** в детальных сценах.

#### Как это работает в JSON

В элементы сцен добавляем правила:

```json
{
  "id": "attitude",
  "label": { "ru": "Как относишься к минету?", "en": "How do you feel about blowjobs?" },

  "skip_if": "body_map.licking.give.genitals",
  "reason": "уже отметил что любит лизать гениталии — вопрос избыточен"
}
```

```json
{
  "id": "intensity",
  "label": { "ru": "Насколько сильно шлёпать?", "en": "How hard to spank?" },

  "prefill_if": "body_map.spanking.receive.ass",
  "prefill_value": "medium_hard",
  "reason": "отметила попу в spanking — значит не против, предлагаем средне-сильно"
}
```

#### Типы правил

| Правило | Что делает | Пример |
|---------|------------|--------|
| `skip_if` | Пропустить вопрос полностью | Не спрашивать "нравится ли минет" если уже отметил |
| `prefill_if` | Показать с уже выбранным ответом (можно изменить) | Интенсивность = средняя, но можно поменять |
| `show_only_if` | Показать только если условие true | Вопрос про глотание только если выбрал "в рот" |

#### Источники данных

| Источник | Что проверяем |
|----------|---------------|
| `body_map.X.Y.Z` | Ответы из body map (licking.give.genitals) |
| `onboarding.X` | Ответы онбординга (oral, rough, power) |
| `this.X` | Ответы из текущей сцены (finish = mouth) |
| `profile.X` | Данные профиля (gender, orientation) |

#### Примеры полного флоу

**Сценарий 1: Body map → blowjob**
1. Юзер в body map отметил "лизать гениталии партнёра" ✓
2. Открывает сцену "blowjob"
3. Вопрос "как относишься к минету?" — **пропущен** (уже ясно что нравится)
4. Сразу видит "какая техника?" и "куда финишировать?"

**Сценарий 2: Body map → spanking**
1. Юзерка в body map отметила "шлёпать по попе" в receive ✓
2. Открывает сцену "spanking-m-to-f"
3. Вопрос "насколько сильно?" — **предзаполнен** как "средне-сильно"
4. Она может оставить или изменить на "легко" / "очень сильно"

---

#### Таблица skip/prefill правил

| Body Map сигнал | Действие | В каких сценах |
|-----------------|----------|----------------|
| `licking.give.genitals` | skip "attitude" | blowjob, cunnilingus |
| `licking.give.feet` | skip "что привлекает" | foot-worship-* |
| `licking.give.ass` | skip "attitude" | rimming-* |
| `spanking.give.ass` | prefill intensity = medium | spanking-* (для M) |
| `spanking.receive.ass` | prefill intensity = medium | spanking-* (для F) |
| `biting.receive.neck` | prefill "грубость ОК" | все rough сцены |
| `light-slapping.receive.face` | skip "готовность" | face-slapping-* |
| `kissing.give.everywhere` | skip "нравится ласкать тело" | massage-*, body-worship-* |

---

### 4.3 Уточнение по SQUIRT

**squirting.json** (role_direction: "solo"):
- Гейт: `body_fluids = YES`
- Для кого: F-пользователь (её опыт сквирта)
- Показывать: всем F кто ответил YES на body_fluids

**squirt-receiving.json** (role_direction: "f_to_m"):
- Гейт: `body_fluids = YES` + `oral = YES`
- Для кого: M-пользователь (хочет принимать её сквирт)
- Логика: если M не любит oral, вряд ли захочет сквирт в лицо/рот

**Body Map связь:**
- Если `licking.receive.genitals` = YES для F → squirting более вероятен (стимуляция)
- Если `licking.give.genitals` = YES для M → squirt-receiving релевантен

---

### 4.4 Уровни интенсивности гейтов

| Уровень | Требование | Что открывает |
|---------|------------|---------------|
| **Базовый** | category = YES | Основные сцены категории |
| **Продвинутый** | category = VERY | Интенсивные вариации (gangbang, cnc, face-slapping) |
| **Комбо** | cat1 + cat2 = YES | Пересечения (rimming = oral+anal, glory-hole = exhib+oral) |
| **Extreme** | cat = VERY + другой cat | Самые жёсткие (breath-play, knife-play, needle-play) |

---

### 4.5 Ещё можно загейтить

Сцены без явного гейта которые стоит связать:

| Сцена | Предложенный гейт | Почему |
|-------|-------------------|--------|
| mutual-masturbation | — | оставить открытым (базовое) |
| positions | — | оставить открытым (базовое) |
| locations | `public` | если не интересует public, зачем локации |
| cheating-fantasy | `roleplay` = VERY | табуированная тема |
| angry-sex | `rough` ИЛИ `quickie` | ✓ уже в матрице |
| electrostim | `toys` + `rough` | ✓ уже в матрице |
| armpit | `body_fluids` | специфичный фетиш запахов |

---

## 5. Итоговый флоу

```
[Регистрация]
       ↓
[Пол + ориентация]
       ↓
[Визуальный онбординг: 16-20 свайпов]
  - Адаптивно по правилам
  - Результат: грубый профиль по категориям
       ↓
[Body Map: 6 активностей × give/receive]
  - Можно адаптивно скрывать по онбордингу
  - Результат: карта зон + подтверждение/уточнение профиля
       ↓
[Вывод: "Тебе может быть интересно: X, Y, Z"]
       ↓
[Пригласить партнёра]
       ↓
[Детальные сцены — только разблокированные]
  - Приоритет по силе интереса (VERY > YES)
  - Некоторые вопросы пропускаются если ответ известен
       ↓
[Мэтчинг с партнёром]
```

---

## 6. Необходимые картинки

### Для 20 категорий × 4 ориентации (гетеро-М, гетеро-Ж, гей, лесби)

| Категория | Гетеро-М | Гетеро-Ж | Гей | Лесби | Итого |
|-----------|----------|----------|-----|-------|-------|
| oral | 1 | 1 | 1 | 1 | 4 |
| anal | 1 | 1* | 1 | 1** | 4 |
| group | МЖЖ | ММЖ | МММ | ЖЖЖ | 4 |
| toys | 1 | 1 | 1 | 1 | 4 |
| roleplay | 1 | 1 | 1 | 1 | 4 |
| quickie | 1 | 1 | 1 | 1 | 4 |
| romantic | 1 | 1 | 1 | 1 | 4 |
| power_dynamic | 1 | 1 | 1 | 1 | 4 |
| rough | 1 | 1 | 1 | 1 | 4 |
| public | 1 | 1 | 1 | 1 | 4 |
| exhibitionism | 1 | 1 | 1 | 1 | 4 |
| recording | 1 | 1 | 1 | 1 | 4 |
| dirty_talk | 1 | 1 | 1 | 1 | 4 |
| praise | 1 | 1 | 1 | 1 | 4 |
| lingerie | 1 | 1 | 1 | 1 | 4 |
| foot | 1 | 1 | 1 | 1 | 4 |
| bondage | 1 | 1 | 1 | 1 | 4 |
| body_fluids | 1 | 1*** | 1 | — | 3 |
| sexting | универс. | — | — | — | 1 |
| extreme | 1 | 1 | 1 | 1 | 4 |

**Итого: ~73 уникальных картинки**

*Гетеро-Ж anal — со страпоном на нём
**Лесби anal — со страпоном
***Лесби body_fluids — не применимо (нет спермы)

---

## 7. Mobile UX (Web App)

> ⚠️ Это веб-приложение, не нативное. Учитываем ограничения браузера.

### 7.1 Визуальный онбординг (Свайпы)

**Основной interaction: свайпы, не кнопки**

| Жест | Значение |
|------|----------|
| Свайп влево | ✗ NO |
| Свайп вправо | ✓ YES |
| Свайп вверх | 🔥 VERY |

**Кнопки**: Опционально как fallback (для тех кто не понял что можно свайпать). Можно вообще без них — Tinder научил всех.

**Веб-специфика:**
- Использовать touch events + pointer events для универсальности
- `touch-action: pan-y` чтобы не мешать скроллу страницы
- Анимация карточки должна следовать за пальцем (drag feedback)

**Картинки:**
- Должны быть понятны без увеличения
- Текст категории: контрастный, с тенью или подложкой
- Формат: WebP с fallback на JPEG

---

### 7.2 Body Map

**Проблема: мелкие зоны на маленьком экране**

Зоны типа "соски", "уши", "пупок", "анус" — пальцем не попасть на 375px экране.

**Решение: Tap → Zoom → Confirm**

```
1. Юзер тапает примерно на область тела
2. Система определяет возможные зоны в радиусе тапа
3. Если одна зона — сразу открыть панель
4. Если несколько (грудь vs соски) — показать выбор:

   ┌─────────────────────────┐
   │  Что ты выбрал(а)?     │
   │                         │
   │  [ Грудь ]  [ Соски ]  │
   │                         │
   └─────────────────────────┘

5. После подтверждения — открыть ZoneActionPanel
```

**Веб-специфика:**
- НЕ использовать pinch-to-zoom (конфликт с браузером)
- `touch-action: none` на canvas/svg силуэта
- `user-select: none` чтобы не выделялся текст
- Viewport meta: `maximum-scale=1` чтобы браузер не зумил

**Альтернатива для совсем мелких зон:**
- Показать список под силуэтом: "Мелкие зоны: Соски, Уши, Пупок, Анус"
- Тап по списку → сразу панель действий

---

### 7.3 ZoneActionPanel (Панель действий)

**Формат: Bottom Sheet**

```
┌─────────────────────────────┐
│                             │  ← drag handle
│  СПИНА                      │
│  ─────────────────────────  │
│                             │
│  Царапать    [❤️] [⭐] [❌]  │
│  Массировать [❤️] [⭐] [❌]  │
│  Целовать    [❤️] [⭐] [❌]  │
│  ...                        │
│                             │
│  [ Готово ]                 │
│                             │
└─────────────────────────────┘
```

**Требования:**
- Tap target: минимум 44×44px для каждой кнопки
- Swipe down для закрытия
- Скролл внутри если много действий
- Safe area padding снизу (iPhone home indicator)

**Веб-специфика:**
- CSS `env(safe-area-inset-bottom)` для iPhone X+
- `overscroll-behavior: contain` чтобы не скроллилась страница за шитом

---

### 7.4 Детальные сцены

**Типы вопросов:**

| Тип | UI компонент | Рекомендация |
|-----|--------------|--------------|
| `multi_select` | Chips (не чекбоксы) | Tap to toggle, визуальный feedback |
| `single_select` | Radio cards | Крупные карточки, не мелкие радио-кнопки |
| `scale` | Discrete slider | 5 делений, не continuous |

**Scale slider веб-специфика:**
```
Не хочу ←─────●─────────→ Обожаю
          1  2  3  4  5
```
- Дискретный (5 значений), не плавный
- Большая touch area для ползунка
- Показывать текущее значение

---

### 7.5 Общие веб-рекомендации

**Viewport:**
```html
<meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no">
```

**Safe areas (iPhone notch/home indicator):**
```css
padding-top: env(safe-area-inset-top);
padding-bottom: env(safe-area-inset-bottom);
```

**Prevent pull-to-refresh:**
```css
body {
  overscroll-behavior-y: contain;
}
```

**Touch feedback:**
```css
.button:active {
  transform: scale(0.95);
  opacity: 0.8;
}
```

**Landscape:**
- Запретить или адаптировать
- Body map в landscape будет странно выглядеть

---

### 7.6 Прогресс и навигация

**Прогресс-бар везде:**
- Онбординг: "5 / 16"
- Body map: "Зона 2 из 6"
- Детальные сцены: "Вопрос 3 из 8"

**Кнопка "Назад":**
- Всегда видна
- Не теряем прогресс при случайном нажатии

**Кнопка "Пропустить":**
- Для body map зон которые не интересны
- Для детальных сцен (skip whole scene)

---

## 8. Skip/Prefill движок (scene-conditions.ts)

### 8.1 Назначение

Модуль `src/lib/scene-conditions.ts` реализует условную логику для элементов детальных сцен:

| Условие | Действие |
|---------|----------|
| `skip_if` | Пропустить вопрос полностью |
| `prefill_if` | Показать с предзаполненным ответом |
| `show_only_if` | Показать только при выполнении условия |

### 8.2 Синтаксис условий

**Body Map:**
```
body_map.{action}.{role}.{zone}
body_map.{action}.{role}.any
```

Примеры:
- `body_map.licking.give.genitals` — true если отметил "лизать гениталии"
- `body_map.spanking.receive.any` — true если что-то отметил в "получать шлепки"

**Onboarding:**
```
onboarding.{category}
onboarding.{category} >= 1
onboarding.{category} == 2
```

Примеры:
- `onboarding.rough` — true если rough > 0 (YES или VERY)
- `onboarding.oral >= 1` — true если oral = YES или VERY
- `onboarding.group == 2` — true только если group = VERY

**Операторы:**
```
condition1 && condition2    // AND
condition1 || condition2    // OR
!condition                  // NOT
```

### 8.3 Пример в JSON сцены

```json
{
  "id": "technique",
  "label": { "ru": "Техника", "en": "Technique" },
  "prefill_if": "body_map.sucking.give.genitals",
  "prefill_value": ["slow_sensual"],
  "prefill_reason": {
    "ru": "Ты уже отметил(а) что любишь сосать — предзаполнено",
    "en": "You already marked you like sucking — prefilled"
  }
}
```

```json
{
  "id": "depth",
  "label": { "ru": "Глубина", "en": "Depth" },
  "show_only_if": "onboarding.oral >= 1",
  "hide_reason": {
    "ru": "Не интересуется оральным сексом",
    "en": "Not interested in oral sex"
  }
}
```

```json
{
  "id": "verbal",
  "label": { "ru": "Что говорит?", "en": "What's said?" },
  "skip_if": "!onboarding.dirty_talk",
  "skip_reason": {
    "ru": "Не интересует dirty talk — пропускаем",
    "en": "Not interested in dirty talk — skipping"
  }
}
```

### 8.4 TypeScript API

```typescript
import {
  evaluateElementConditions,
  processSceneElements,
  getVisibleElements,
  type ConditionContext
} from '@/lib/scene-conditions';

// Контекст с данными юзера
const context: ConditionContext = {
  bodyMap: {
    licking: { give: ['genitals', 'neck'], receive: ['neck'] },
    spanking: { give: [], receive: ['ass'] }
  },
  onboarding: {
    oral: 2,      // VERY
    rough: 1,     // YES
    dirty_talk: 0 // NO
  }
};

// Проверить один элемент
const result = evaluateElementConditions(element, context, 'ru');
// { skip: false, prefill: ['slow_sensual'], show: true, ... }

// Обработать все элементы сцены
const processed = processSceneElements(scene.elements, context, 'ru');
// Каждый элемент получает conditionResult

// Получить только видимые
const visible = getVisibleElements(scene.elements, context);
```

### 8.5 Utility-функции для построения условий

```typescript
import {
  bodyMapCondition,
  onboardingCondition,
  and,
  or,
  not
} from '@/lib/scene-conditions';

// Строим условия программно
const cond1 = bodyMapCondition('licking', 'give', 'genitals');
// "body_map.licking.give.genitals"

const cond2 = onboardingCondition('oral', '>=', 1);
// "onboarding.oral >= 1"

const combined = and(cond1, cond2);
// "body_map.licking.give.genitals && onboarding.oral >= 1"

const negated = not(onboardingCondition('rough'));
// "!onboarding.rough"
```
